<% if user_signed_in? && current_user.isAdmin %>
<h1>Use mp4/webm format to the player!</h1>
  <div class='contrainer-fluid center'>
    <div class='articles-box'>
    <%= render 'form' %>
    </div>
    <%= link_to 'Back', articles_path %>
  </div>
<% else %>
  <h1>Access denied</h1
<% end %>